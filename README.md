# Fleet: Test

GitOps repository for fleet `8b5f4006-cbc3-492d-ba4a-1d22d2df990f`.
Managed by ingress-poc management-api.

## Structure

- `fleets/` — Fleet CRD manifests
- `routes/` — Route CRD manifests
- `lambdas/` — Lambda CRD manifests
